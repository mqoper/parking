<table>
  <thead>
  <tr>
    <th>Spot name</th>
  </tr>
  </thead>

  <tbody>
  <% @histories.each do |history| %>
    <% if history.spot.reserved == true %>
      <tr>
        <td><%= history.spot.name %></td>
        <td><%= link_to 'Edit', edit_spot_path(history.spot) %></td>
      </tr>
    <% end %>
  <% end %>
  </tbody>
</table>